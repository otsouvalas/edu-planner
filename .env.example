# Dev: copy to server/.env (or export in your shell) before starting the server.
# See server/.env.example for the full list.
# For the Docker deployment see .env.production.example instead.
ANTHROPIC_API_KEY=
DATABASE_URL=file:./dev.db
